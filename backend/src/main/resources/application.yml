spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}

  jpa:
    show-sql: false
    properties:
      hibernate:
        jdbc:
          time_zone: America/Argentina/Buenos_Aires

app:
  storage:
    local:
      path: uploads