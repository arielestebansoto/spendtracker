server:
  servlet:
    session:
      cookie:
        http-only: true
        secure: true
        same-site: lax
app:
  cors:
    allowed-origins: https://spendtracker.com

logging:
  level:
    root: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN