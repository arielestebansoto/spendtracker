server {

    listen 80;

    return 301 https://$host$request_uri;
}

server {

    listen 443 ssl http2;

    ssl_certificate '';
    ssl_certificate_key '';

    include /etc/nginx/common/locations.conf;
}